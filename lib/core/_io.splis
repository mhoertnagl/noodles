;; Input/Output routines.
(do
  ;; TODO: Doc.
  (defmacro prints [& args] (write *STDOUT* @args))

  ;; TODO: Doc.
  (defmacro printsln [& args] (write *STDOUT* @args "\n"))

  (defmacro print [& args] (prints @(map str args)))

  (defmacro println [& args] (printsln @(map str args)))
)
